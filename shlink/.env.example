# Shlink stack environment (copy to .env and fill in)
# DO NOT COMMIT .env

# Public-facing config
DEFAULT_DOMAIN=example.com
IS_HTTPS_ENABLED=true
SHLINK_PORT=8080

# GeoLite2 license key for IP geolocation (optional, but useful)
# Obtain from MaxMind and keep it secret.
GEOLITE_LICENSE_KEY=YOUR-GEOLITE2-LICENSE-KEY

# Database credentials (used by both shlink and mysql)
MYSQL_PASSWORD=change_me_shlink_db_password
MYSQL_ROOT_PASSWORD=change_me_mysql_root_password
DB_USER=shlink_user


# Path Configuration (defaults if unset: /mnt/data and /srv)
DATA_ROOT=/mnt/data
# LOCAL_ROOT=/srv
