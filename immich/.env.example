# Immich stack configuration
# Immich app version tag (optional)
IMMICH_VERSION=release

# Upload location mounted into the containers
UPLOAD_LOCATION=/mnt/data/immich/uploads

# Postgres connection details used by Immich
DB_USERNAME=immich
DB_PASSWORD=change_me
DB_DATABASE_NAME=immich
